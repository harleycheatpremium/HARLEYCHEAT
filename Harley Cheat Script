-- GODZILLA MOD | HARLEY PREMIUM EDITION
-- Loaded: Anti-Cheat Bypass v2.0
local OrionLib = loadstring(game:HttpGet(('https://raw.githubusercontent.com/shlexware/Orion/main/source')))()
local Window = OrionLib:MakeWindow({Name = "GODZILLA HACK | HARLEY EDITION", HidePremium = false, SaveConfig = true, ConfigFolder = "GodzillaConfig"})

-- SECURE BYPASS
local mt = getrawmetatable(game)
setreadonly(mt, false)
local oldNamecall = mt.__namecall
mt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()
    if method == "FireServer" and tostring(self):lower():find("anticheat") then return nil end
    return oldNamecall(self, ...)
end)
setreadonly(mt, true)

-- TAB: COMBAT
local CombatTab = Window:MakeTab({Name = "Combat", Icon = "rbxassetid://4483345998", PremiumOnly = false})
CombatTab:AddToggle({
    Name = "Silent Aim (Headshot)",
    Default = false,
    Callback = function(Value) getgenv().SilentAim = Value end
})

-- TAB: VISUALS
local VisualTab = Window:MakeTab({Name = "Visuals", Icon = "rbxassetid://4483345998", PremiumOnly = false})
VisualTab:AddToggle({
    Name = "ESP Box (Full)",
    Default = false,
    Callback = function(Value)
        for _,v in pairs(game.Players:GetPlayers()) do
            if v ~= game.Players.LocalPlayer and v.Character then
                local h = v.Character:FindFirstChild("Highlight") or Instance.new("Highlight", v.Character)
                h.Enabled = Value
            end
        end
    end
})

-- TAB: PLAYER (GOD MODE)
local PlayerTab = Window:MakeTab({Name = "God Mode", Icon = "rbxassetid://4483345998", PremiumOnly = false})
PlayerTab:AddSlider({
    Name = "Speed Hack",
    Min = 16, Max = 150, Default = 16,
    Callback = function(Value) game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Value end
})

PlayerTab:AddButton({
    Name = "Kill All (Global)",
    Callback = function()
        -- Godzilla Hack tarzı global trigger
        for _,v in pairs(game.Players:GetPlayers()) do
            if v ~= game.Players.LocalPlayer then
                -- Burada oyunun kendi "Damage" eventini tetiklemelisin
            end
        end
    end
})

OrionLib:Init()
