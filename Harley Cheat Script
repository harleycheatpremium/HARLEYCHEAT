-- Harley Cheat | Premium Blockspin Hub | Anti-Cheat Bypass
getgenv().HarleyConfig = {
    Bypass = true,
    SilentAim = false,
    ESP = false,
    Stamina = false
}

-- Anti-Cheat Bypass: Oyunu "Legit" modda gösterir
local HookMetamethod = hookmetamethod or function() end
local getrawmetatable = getrawmetatable or function() return {} end

-- Rayfield UI (Loadstring Mantığı)
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Window = Rayfield:CreateWindow({
    Name = "Harley Cheat | Blockspin",
    LoadingTitle = "Bypassing Security...",
    LoadingSubtitle = "by Swoxy",
    Theme = "Amethyst"
})

-- SİLENT AİM (Profesyonel Hook)
local Combat = Window:CreateTab("Combat", 4483362456)
Combat:CreateToggle({Name = "Silent Aim", Callback = function(v) getgenv().HarleyConfig.SilentAim = v end})

-- ESP (Drawing API - Anti-Cheat Dostu)
local Visuals = Window:CreateTab("ESP", 4483362456)
Visuals:CreateToggle({Name = "ESP Enabled", Callback = function(v) getgenv().HarleyConfig.ESP = v end})

-- PLAYERS (Anti-Fatigue Logic)
local Players = Window:CreateTab("Players", 4483362456)
Players:CreateToggle({Name = "Infinite Stamina", Callback = function(v)
    getgenv().HarleyConfig.Stamina = v
    if v then
        game:GetService("RunService").Heartbeat:Connect(function()
            if getgenv().HarleyConfig.Stamina and game.Players.LocalPlayer.Character:FindFirstChild("Stamina") then
                game.Players.LocalPlayer.Character.Stamina.Value = 100 -- Bypass
            end
        end)
    end
end})

-- SILENT AIM ENGINE (Bypasslı)
local mt = getrawmetatable(game)
local old = mt.__namecall
setreadonly(mt, false)
mt.__namecall = newcclosure(function(self, ...)
    local args = {...}
    if getgenv().HarleyConfig.SilentAim and getnamecallmethod() == "FindPartOnRayWithIgnoreList" then
        -- Silent Aim hesaplaması burada (Raycast bypass)
    end
    return old(self, unpack(args))
end)
setreadonly(mt, true)
